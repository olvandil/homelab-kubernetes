apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: nginx-test
  namespace: nginx-test

spec:
  secretName: nginx-test-tls

  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer

  dnsNames:
  - nginx-test.watworld.uk